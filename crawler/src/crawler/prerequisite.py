from __future__ import annotations


def crawl_prerequisites() -> None:
    raise NotImplementedError(
        "Prerequisite/corollary request parameters must be captured from browser DevTools before crawling."
    )

